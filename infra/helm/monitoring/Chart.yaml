apiVersion: v2
name: monitoring
description: Prometheus + Grafana monitoring with Kafka and PostgreSQL dashboards
type: application
version: 0.1.0

dependencies:
  - name: kube-prometheus-stack
    version: "58.4.0"
    repository: "https://prometheus-community.github.io/helm-charts"
